/**
 * Persistence Adapter (Output/Driven Adapter) - Database Access.
 * 
 * <p>This package contains JPA entities, Spring Data repositories,
 * and adapter implementations that handle database persistence.
 * Adapters implement the output ports defined in the domain layer.</p>
 * 
 * <p>This package is organized into sub-packages:</p>
 * <ul>
 *   <li>{@code entity} - JPA entity classes</li>
 *   <li>{@code repository} - Spring Data JPA repository interfaces</li>
 *   <li>{@code adapter} - Output port implementations</li>
 *   <li>{@code mapper} - Entity/Domain model mappers</li>
 * </ul>
 * 
 * @since 1.0.0
 */
package de.nofelix.inventorybackend.adapter.out.persistence;
